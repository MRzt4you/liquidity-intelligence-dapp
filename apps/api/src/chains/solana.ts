import WebSocket from 'ws';import {NormalizedEvent} from '../types';
export class SolanaAdapter{constructor(private cb:(x:NormalizedEvent)=>void){}start(){const url=process.env.SOLANA_WS_URL;if(!url)return;let delay=1000;const connect=()=>{const ws=new WebSocket(url);ws.on('open',()=>{delay=1000;ws.send(JSON.stringify({jsonrpc:'2.0',id:1,method:'logsSubscribe',params:['all',{commitment:'confirmed'}]}));this.cb({type:'chain_status',chain:'solana',source:'solana-rpc',ts:Date.now()})});ws.on('message',raw=>{try{const m=JSON.parse(raw.toString());if(m.method!=='logsNotification')return;const x=m.params?.result;this.cb({type:'log',chain:'solana',source:'solana-rpc',ts:Date.now(),txHash:x?.value?.signature,slot:x?.context?.slot,raw:x})}catch{}});ws.on('close',()=>{setTimeout(connect,delay);delay=Math.min(delay*2,30000)});ws.on('error',()=>ws.close())};connect()}}
