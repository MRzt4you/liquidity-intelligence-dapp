export const cfg=()=>({maxPositionUsd:Number(process.env.MAX_POSITION_USD||100),maxDailyLossUsd:Number(process.env.MAX_DAILY_LOSS_USD||100),maxOpenPositions:Number(process.env.MAX_OPEN_POSITIONS||3),minLiquidityUsd:Number(process.env.MIN_LIQUIDITY_USD||15000),maxHolderConcentrationPct:Number(process.env.MAX_HOLDER_CONCENTRATION_PCT||35),maxSlippageBps:Number(process.env.MAX_SLIPPAGE_BPS||100)});
export function riskGate(i:any,c=cfg()){const r:string[]=[];if(i.positionUsd>c.maxPositionUsd)r.push('POSITION_LIMIT');if(i.openPositions>=c.maxOpenPositions)r.push('OPEN_POSITION_LIMIT');if(i.dailyLossUsd>=c.maxDailyLossUsd)r.push('DAILY_LOSS_LIMIT');if(i.liquidityUsd<c.minLiquidityUsd)r.push('LOW_LIQUIDITY');if(i.holderConcentrationPct>c.maxHolderConcentrationPct)r.push('HOLDER_CONCENTRATION');if(i.slippageBps>c.maxSlippageBps)r.push('SLIPPAGE');if(i.risk==='CRITICAL')r.push('CRITICAL_RISK');return{allowed:r.length===0,reasons:r}}
